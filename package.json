{
  "name": "3d-world-jungle",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "description": "Stroll through a low-poly / isometric 3D jungle with Three.js",
  "scripts": {
    "dev": "vite",
    "build:textures": "node scripts/generate-textures.mjs",
    "build:assets": "node scripts/fetch-assets.mjs",
    "build:models": "node scripts/fetch-nature-models.mjs",
    "build:models:fetch": "node scripts/fetch-nature-models.mjs --fetch",
    "build:models:all": "node scripts/fetch-nature-models.mjs --fetch --all",
    "build": "npm run build:textures && npm run build:assets && npm run build:models && vite build",
    "build:full": "npm run build:textures && node scripts/fetch-assets.mjs --fetch && node scripts/fetch-nature-models.mjs --fetch && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
