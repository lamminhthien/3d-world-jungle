{
  "name": "3d-world-jungle",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "description": "Stroll through a low-poly / isometric 3D jungle with Three.js",
  "scripts": {
    "dev": "vite",
    "build:textures": "node scripts/generate-textures.mjs",
    "build:assets": "node scripts/fetch-assets.mjs",
    "build": "npm run build:textures && npm run build:assets && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
